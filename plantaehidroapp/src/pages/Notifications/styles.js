import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  createAlertButton: {
    borderRadius: 20,
    borderWidth: 1,
    borderColor: '#000',
    width: '50%',
    alignSelf: 'center',
    marginTop: '4%',
  },
});

export default styles;
